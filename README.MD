# With the command [`npm i`] the necessary packages for the project are installed

with the command `npm start` the backend server starts

For more information consult the documentation in the path `/api-docs/`
